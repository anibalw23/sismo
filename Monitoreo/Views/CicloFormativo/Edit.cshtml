@model Monitoreo.Models.CicloFormativo

@{
    //ViewBag.Title = String.Format(Resources.T.ResourceManager.GetString("EditandoX"), Resources.T.ResourceManager.GetString("CicloFormativo"));
    ViewBag.Title = "Editar Actividades Formativas"; 
}

<div class="panel-heading">
    <h3>@ViewBag.Title</h3>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="panel-body">
    <div class="form-horizontal">
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

                @*<div class="form-group">
                    @Html.LabelFor(model => model.SuperCicloFormativoId, "Ciclo Formativo", new { @class = "control-label col-sm-3" })
                    <div class="col-sm-4">
                        @Html.DropDownListFor(x => x.SuperCicloFormativoId, (IEnumerable<SelectListItem>)ViewBag.SuperCicloFormativoId, new { @class = "form-control" })
                        @Html.ValidationMessageFor(model => model.SuperCicloFormativoId)
                    </div>
                </div>*@
                @Html.EditorFor(model => model.Tema)        
                @Html.EditorFor(model => model.tipo)
                @Html.EditorFor(model => model.DuracionTotal)
                @Html.EditorFor(model => model.FechaInicio)
                @Html.EditorFor(model => model.FechaFinalizacion)

                @Html.EditorFor(model => model.Nivel)
                @Html.EditorFor(model => model.Area)
                @Html.EditorFor(model => model.Ciclo)


        @*<div class="form-group">
            @Html.Label("Escuela", new { @class = "control-label col-sm-3" })
            <div class="col-sm-4">
                @Html.DropDownList("CentrosId", (IEnumerable<SelectListItem>)ViewBag.CentrosId, new { @class = "form-control" })
            </div>
        </div>*@

        </div>
    </div>
    <div class="panel-footer">
        <button type="submit" value="Save" class="btn btn-primary">
            <span class="glyphicon glyphicon-floppy-disk" aria-hidden="true"></span> @Resources.T.ResourceManager.GetString("Guardar")
        </button>
        <script>
            document.write('<a href="' + document.referrer + '" class="btn btn-default">' +
                '<span class="glyphicon glyphicon-remove" aria-hidden="true"></span> @Resources.T.ResourceManager.GetString("Cancelar")</a>');
        </script>
        <a href="@Url.Action("Delete", new { id = Model.Id })" class="btn btn-danger">
            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> @Resources.T.ResourceManager.GetString("Borrar")
        </a>
    </div>
    <script type="text/javascript">
    (function ($) {
        $("#FormadorId").select2({
            placeholder: "Seleccione...",
            allowClear: true
        });
        $("#AcompananteId").select2({
            placeholder: "Seleccione...",
            allowClear: true
        });
        $("#TutorVirtualId").select2({
            placeholder: "Seleccione...",
            allowClear: true
        });
    })(jQuery);
    </script>
}


@section Actions {
    <ul class="nav nav-stacked">
        <li>@Html.ActionLink(Resources.T.ResourceManager.GetString("Volver"), "Index")</li>
    </ul>
}

